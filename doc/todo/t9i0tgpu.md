# `t9i0tgpu` Implement simple builin functions

Approximate function list:

  - (predef) math: [`+`, `-`, `*`, `/`]
  - (predef) comparison: [`=`, `<`, `>`, `<=`, `>=`, `!=`]
  - (lib) io: [`print`, `open`, `scan`]
  - (lib) string: [`join`, `split`, `format`]
