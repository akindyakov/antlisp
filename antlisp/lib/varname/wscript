#!/usr/bin/env python
# encoding: utf-8


def build(ctx):
    ctx.stlib(
        target="antlisp-lib-varname",
        source = [
            "prefix.cpp",
        ],
        use=[
            "antlisp-lib-cell",
            "antlisp-lib-util",
        ],
    )
    ctx.program(
        target="prefix_ut",
        features="test",
        source="prefix_ut.cpp",
        use=[
            "antlisp-lib-varname"
        ],
    )
