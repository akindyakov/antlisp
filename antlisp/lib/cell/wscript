#!/usr/bin/env python
# encoding: utf-8


def build(ctx):
    ctx.stlib(
        target="antlisp-lib-cell",
        source = [
            "cell.cpp",
        ],
        use=[
            "antlisp-lib-util",
        ],
    )

    ctx.program(
        target="cell_ut",
        source="cell_ut.cpp",
        use=[
            "antlisp-lib-cell"
        ],
    )
    ctx(
        rule="${SRC}",
        source="cell_ut",
    )
